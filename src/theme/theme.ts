import { createTheme } from '@mui/material';

const theme = createTheme({
  spacing: 10,
  palette: {
    primary: {
      main: '#0D0D0D',
      contrastText: '#ffffff',
    },
  },
});

export default theme;
